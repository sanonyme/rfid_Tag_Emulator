import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.edge.rfidemulator',
  appName: 'Zeus RFID Emulator',
  webDir: 'dist'
};

export default config;
